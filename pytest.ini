[pytest]
# 将 backend 加入测试进程的模块搜索路径，使 ``from app...`` 与生产启动方式保持一致。
pythonpath = backend
testpaths = backend/tests
markers =
    live: 连接运行中的真实后端和 PostgreSQL。
    generation: 调用真实模型生成文章和图片，会消耗 API 配额并保存会话。
