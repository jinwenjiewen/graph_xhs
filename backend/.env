# SQLAlchemy async URL (application database)
DATABASE_URL=postgresql+asyncpg://postgres:root@localhost:5432/aicontent

# Optional. LangGraph's AsyncPostgresSaver uses psycopg, not asyncpg.
# If omitted, it is derived from DATABASE_URL automatically.
# CHECKPOINT_DATABASE_URL=postgresql://postgres:root@localhost:5432/aicontent
